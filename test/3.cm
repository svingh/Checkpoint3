/*
Checks that two things are the same type:
- match two operands
 -match two sides of an assignment
*/

void foo (void) {
    int x;
    x = 3;
    output(x);
}

int fun(int fff ) {
     int x; 
     int y;
     x = x * 2 + 1;
     y = x + foo();

     x = foo();
     
     return y;
}